// ── STEP 1: Firebase console → Project settings → Your apps → Web app → copy the config values here ──
window.ISI_CONFIG = {
  apiKey: "PASTE_ME",
  authDomain: "PASTE_ME.firebaseapp.com",
  projectId: "PASTE_ME",
  storageBucket: "PASTE_ME.appspot.com",
  messagingSenderId: "PASTE_ME",
  appId: "PASTE_ME"
};
// ── STEP 2: pick any private family word — must be IDENTICAL on every device (it names your shared data) ──
window.FAMILY_ID = "iron-family";
